name:
  type: string
  required: true

type:
  type: string
  required: true
  allowed: ['Standard', 'Starter', 'Extra']

index: # release order across all collections
  type: string
  nullable: true
  coerce: str
