set_allow:
  - '*'

regulation_allow:

oracle_ban:

oracle_restrict:

oracle_exception:
  Basic Energy: 99

oracle_limit: 4

list_limit:
  main:
    minimum: 60
    maximum: 60
  side:
    maximum: 10
