[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ptcg-db"
version = "0.0.0"
dependencies = [
    "ruamel.yaml",
    "cerberus"
]

[tool.setuptools.packages.find]
include = []
exclude = ["data", "script", "schema"]